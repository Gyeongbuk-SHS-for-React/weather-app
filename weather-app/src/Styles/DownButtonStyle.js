import styled from "styled-components"

const DownButtonStyle = styled.div`
  & {
    font-size: 40px;
    text-align: center;
    position: relative;
    bottom: 58px;
    display: inline-block;
  }
  & :hover {
    cursor: pointer;
  }
`
export default DownButtonStyle
